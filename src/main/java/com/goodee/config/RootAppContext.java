package com.goodee.config;

import org.springframework.context.annotation.Configuration;

// 프로젝트 작업 시 사용할 bean을 정의하는 클래스
@Configuration
public class RootAppContext {
	// 나중에 @Bean으로 선언해서 임시로 쓰고자하는 빈을 만들때 쓰는 곳 (정작 중요한것들은 여기에 선언 X)
	
}
